%h1 Listing items

%table
  %thead
    %tr
      %th Name
      %th Description
      %th Price
      %th Image url
      %th
      %th
      %th

  %tbody
    - @items.each do |item|
      %tr
        %td= link_to item.name, item
        %td= item.description
        %td= "#{item.price} yen"
        %td= item.image_url
        %td= link_to 'Show', item
